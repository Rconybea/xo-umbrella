;; see (info "(emacs) directory variables")

;;((c++-mode . ((projectile--cmake-manual-command-alist . ((:configure-command "cmake -DCMAKE_INSTALL_PREFIX=~/scratch -DCMAKE_BUILD_TYPE=debug -B .build")
;;                                                         (:compile-command "cmake --build .build")
;;                                                         (:test-command . "cmake --build .build --target unit-tests"))))))
